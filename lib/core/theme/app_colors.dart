import 'package:flutter/material.dart';

class AppColors {
  // Primary (océan)
  static const Color primary = Color(0xFF0A6C74);
  static const Color secondary = Color(0xFF14B8A6);

  // Neutrals
  static const Color background = Color(0xFFF6F8FA);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);

  // Dark
  static const Color darkBackground = Color(0xFF020617);
  static const Color darkSurface = Color(0xFF020617);

  static const Color primaryBlue = Color(0xFF083E6D); // bleu principal
  static const Color secondaryBlue = Color(0xFF90CAF9); // bleu clair
}
